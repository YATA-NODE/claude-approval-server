# 性能基準(案C Phase 0、暫定確定)

**位置づけ**: production は本 Phase で未変更(HEAD = bf74706)。ここで測るのは
「1 フレームあたりの dialog 判定(frameOf + readTabBarRow + barRowIsCliDrawn の 1 サイクル)」の
**現行の絶対時間**であり、Phase 2 で案C を実装したあと同一条件で再測して比較する基準値にする
(master plan: 「Phase 2 は同一基準の再検証に限定〔基準の後決めをしない〕」)。

## 測定コマンド

```
node tools/bench-frame.js --raw e2e-raw-t4-tabbed.log --cols 120 --rows 40 --chunk 512 --warmup 1 --reps 3
```

## 対象録画

| 項目 | 値 |
|---|---|
| ファイル | `e2e-raw-t4-tabbed.log`(gitignore・ローカルのみ。docs/attr-manifest.json に同一 sha256 のエントリあり) |
| sha256 | `0273f3c4aedb556d4fc2d7a29c3699c54999dfb0acdfb326a443447cf15f801a` |
| 幾何 | cols=120 rows=40 chunk=512 |
| フレーム数(この幾何での再生) | 27 |

## 基準 commit

| 項目 | 値 |
|---|---|
| commit(短縮) | `bf74706` |
| commit(完全) | `bf7470603384ae696cce2f1f13ee4b1c97d14b8f` |
| node | `v20.20.2` |
| 測定日時 | 2026-08-13T07:10:00.624Z |
| production 変更 | なし(Phase 0 は観測専用。この値が「現行 HEAD の絶対時間」の基準) |

## warm-up 条件

- warm-up 1 回(計測に含めない、JIT ウォームアップ目的)→ 本計測 3 反復。
- 各反復は録画全体を @xterm/headless に再生し、フレームごとに dialog 判定 3 関数を呼んで
  `process.hrtime.bigint()` で判定サイクルのみを計測(端末への write コストは含めない)。
  反復内の全フレームの合計時間 ÷ フレーム数 = その反復の「1 フレーム平均」。

## 3 反復の生値 + 中央値

| 反復 | 1 フレーム平均(ms) |
|---|---|
| 1 | 0.7561 |
| 2 | 0.5288 |
| 3 | 0.5247 |
| **中央値** | **0.5288** |

## 絶対上限の暫定値

- master plan 初期値: **1 フレーム ≤ 50ms**。
- 実測中央値 0.5288ms は初期値を**下回った**。
- 実測が下回ったため、暫定上限は master plan 初期値の **50ms を維持**しつつ、
  実測値ベースの上限提案(安全マージン込み)を併記する: **2.12ms**(中央値の 4 倍、
  master plan の「HEAD 比 2 倍以内」要求とは別軸 = Phase 2 実装後の絶対時間比較用の参考値)。
  最終的な絶対上限の確定は Phase 1 の設計判断に委ねる(ここでは実測と提案値の併記に留める)。

## suite 完走の確認

- 3/3 反復が完走し、各反復で 27 フレームが計測された(反復間で一致)。
- 0 フレーム計測 or 反復数不足の場合、本ツールはこの節を書かずに exit 1 する(このファイルが
  存在すること自体が「suite が完走した」ことの証跡)。
